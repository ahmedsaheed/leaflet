import path from 'path';

export const TopBar = ({parentDir, name}) => {
  return (
  <div
    style={{
      // display: "flex",
      display: "none",
      justifyContent: "space-between",
      alignItems: "center",
      fontSize: "13.5px",
    }}
  >
</div>


  )
};
